# Cleaned requirements for Docker base image
# Trimmed from user's pip freeze and adjusted to resolve conflicts

# Core
addict==2.4.0
certifi==2022.12.7
cffi==1.17.1
charset-normalizer==2.1.1
click==8.1.8
colorama==0.4.6
decorator==4.4.2
filelock==3.14.0
idna==3.4
Jinja2==3.1.4
MarkupSafe==2.1.5
packaging==24.2
pillow==10.2.0
pycparser==2.22
PyYAML==6.0.2
requests==2.28.2
urllib3==1.26.13
typing_extensions==4.7.1
six==1.16.0

# Numerical / scientific
# numpy: allow pip to resolve a compatible numpy for other packages; keep <1.27 to stay
# compatible with scipy 1.10.x while allowing newer 1.24/1.25/1.26 micro releases
numpy>=1.24.1,<1.27.0
scipy==1.10.1
pandas==2.0.3
scikit-learn==1.7.2

# MMAction2 & OpenMMLab
mmengine==0.10.7
mmcv==2.1.0
openmim==0.3.9

# Video / image
# pin opencv-python to avoid the breaking 4.12+ requirement that needs numpy>=2.x
# allow recent 4.x releases that remain compatible with numpy 1.x
opencv-python>=4.6.0,<4.12
decord==0.6.0
einops==0.8.1
moviepy==1.0.3
imageio==2.35.1
imageio-ffmpeg==0.5.1

# Visualization
matplotlib==3.7.5
contourpy==1.1.1
fonttools==4.57.0
kiwisolver==1.4.7
pyparsing==3.1.4
cycler==0.12.1

# Utilities
tqdm==4.65.2
rich==13.4.2
tabulate==0.9.0
termcolor==2.4.0
markdown-it-py==3.0.0
Markdown==3.7

# Filesystems / IO
fsspec==2024.6.1

# Notes:
# - PyTorch and torchvision are provided by the base image; do NOT include torch/torchvision/torchaudio.
# - Avoid including Windows-specific or editable/local entries.
# - If pip still fails during resolution, loosen pinned versions for numpy/opencv or remove the explicit numpy pin.